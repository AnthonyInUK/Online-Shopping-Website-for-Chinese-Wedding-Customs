import { WeddingShopValidators } from './wedding-shop-validators';

describe('WeddingShopValidators', () => {
  it('should create an instance', () => {
    expect(new WeddingShopValidators()).toBeTruthy();
  });
});
